[options]
addons_path = /odoo/addons,/mnt/custom-modules
data_dir = /var/lib/odoo
admin_passwd = admin_dev_only
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo
db_name = False
xmlrpc_port = 8069
logfile = False
log_level = info

; dev_mode se pasa por linea de comandos (ver command en docker-compose.yml).
; Odoo 17 solo lee --dev desde la CLI: si se define aqui, se ignora.
